import { Link, router } from '@inertiajs/react';
import WebsiteLayout from '@/Layouts/WebsiteLayout';
import { Icon } from '@/Components/Icons';
import { useI18n } from '@/i18n';
export default function Products({settings, products, filters={}}){const {locale}=useI18n(); const bn=locale==='bn'; const search=(e)=>{e.preventDefault(); router.get('/collections',{search:e.target.search.value,type:e.target.type.value},{preserveState:true})}; return <WebsiteLayout settings={settings}><section className="mx-auto max-w-7xl px-4 py-16 md:px-6"><div className="flex flex-col gap-5 md:flex-row md:items-end md:justify-between"><div><p className="font-black text-blue-600">{bn?'কালেকশন':'Collection'}</p><h1 className="mt-3 text-4xl font-black">{bn?'গ্লাস, ফ্রেম ও চোখের পণ্য':'Glasses, frames and eye care products'}</h1></div><form onSubmit={search} className="flex gap-2"><input name="search" defaultValue={filters.search??''} className="rounded-2xl border px-4 py-3" placeholder={bn?'সার্চ':'Search'}/><select name="type" defaultValue={filters.type??''} className="rounded-2xl border px-4 py-3"><option value="">All</option><option value="glass">Glass</option><option value="medicine">Medicine</option></select><button className="rounded-2xl bg-blue-600 px-5 font-black text-white">{bn?'খুঁজুন':'Find'}</button></form></div><div className="mt-10 grid gap-6 md:grid-cols-4">{products.data.map(p=><article key={p.id} className="overflow-hidden rounded-[2rem] border border-slate-200 bg-white shadow-sm"><div className="h-52 bg-slate-100">{p.images?.[0]?.url?<img src={p.images[0].url} className="h-full w-full object-cover"/>:<div className="grid h-full place-items-center text-blue-600"><Icon name="box" className="h-12 w-12"/></div>}</div><div className="p-5"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-black text-blue-700">{p.product_type}</span><h3 className="mt-3 text-lg font-black">{p.name}</h3><p className="mt-2 text-sm text-slate-500">{bn?(p.website_short_description_bn||p.website_short_description):p.website_short_description}</p><div className="mt-4 text-xl font-black">৳ {Number(p.sale_price).toFixed(2)}</div></div></article>)}</div><div className="mt-8 flex gap-2">{products.links.map((l,i)=><Link key={i} href={l.url??'#'} className={`rounded-xl px-4 py-2 ${l.active?'bg-blue-600 text-white':'border'}`} dangerouslySetInnerHTML={{__html:l.label}} />)}</div></section></WebsiteLayout>}
